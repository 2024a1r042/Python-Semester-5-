# 12. Write a python program to take a password and check whether it contains a @ and has atleast 8 char
password = input("Enter a password:")
password1 = password.find("@")
print(password1)
print(len(password)>=8)
