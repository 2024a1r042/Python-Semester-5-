# 5. Take a password and check length, presence of @, and whether first and last characters are different
pw = input("Enter the password:")
print("Length atleast 8 characters:", len(pw)>=8)
print("Contains @:", "@" in pw)
print("First and Last different:", pw[0]!=pw[-1])
