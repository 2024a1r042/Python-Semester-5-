# 8.Write a python program to repeatedly calculate the sum of digits of a number until the result becomes a single digit

num = int(input("Enter a number: "))

print(f"Original number: {num}")

while num >= 10:
    digit_sum = 0
    temp = num
    while temp > 0:
        digit_sum += temp % 10
        temp = temp // 10
    num = digit_sum

print(f"The single-digit sum is: {num}")
