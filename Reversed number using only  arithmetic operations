# 5. Write a python program to input a number and reverse it using arithmetic operations only
num = int(input("Enter a number: "))

is_negative = num < 0
n = abs(num)
reversed_num = 0
while n > 0:
  digit = n % 10
  reversed_num = (reversed_num * 10) + digit
  n = n // 10
if is_negative:
  reversed_num = -reversed_num
print(f"The reversed number is: {reversed_num}")
